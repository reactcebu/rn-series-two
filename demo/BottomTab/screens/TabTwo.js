import React from 'react';
import {View, Text} from 'react-native';

const TabTwo = () => {
    return (
        <View style={{flex:1, justifyContent: 'center', alignItems: 'center'}}>
            <Text style={{fontSize: 18}}>Tab Two</Text>
        </View>
    );
};

export default TabTwo;